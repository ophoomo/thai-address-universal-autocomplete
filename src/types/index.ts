/**
 * @module types
 * Shared TypeScript types and interfaces for the thai-address-autocomplete library.
 *
 * All types are re-exported from this barrel file to provide a single import point.
 */

export type {
  ThaiAddressSuggestion,
  AddressField,
  AutocompleteOptions,
  AutocompleteInstance,
  AutocompleteState,
  ClassNames,
  RenderItemFn,
  RenderContainerFn,
  SourceFn,
  OnSelectFn,
  AdapterOptions,
  AdapterSearchFn,
  KeyboardAction,
} from './autocomplete';
